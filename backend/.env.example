PORT=4000
NODE_ENV=development

# Supabase - Project Settings > API
SUPABASE_URL=https://tzsnqcyqcdocjydquacr.supabase.co
SUPABASE_SERVICE_KEY=pegar_service_role_key_aqui

# JWT
JWT_SECRET=cambiar_por_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# CORS
FRONTEND_URL=http://localhost:5173

# Microservicio ML (FastAPI). Si se omite, el backend usa el stub local.
ML_SERVICE_URL=http://localhost:8000

# Asistente de IA (Google Gemini). Necesario para el cuestionario de nivel
# socioeconómico y la recomendación de intervenciones. Sin esta llave, esos
# endpoints responden 503. Obtén una en https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest

# Notificaciones (opcionales). Si se omiten, las alertas se registran en consola.
# Email vía SendGrid:
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=
# SMS vía Twilio:
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
